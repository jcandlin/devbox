
puppet agent --test to deploy .war file (run from agent)

service puppetmaster status
service puppetmaster start
service puppetmaster restart
puppet apply -e "include apache"
puppet cert clean pp3ux596.williamhill.local
puppet cert list -all
puppet cert clean -all
puppet module search gcc
puppet module install puppetlabs-gcc
puppet cert --list

puppetd --test --pluginsync

Restart App
-----------
puppet apply -e "class tcrestart {tcserver::stop{'tcserverstop': instanceName => 'orion', instanceUser => 'brdg_usr', } tcserver::start{'tcserverstart': instanceName => 'orion', instanceUser => 'brdg_usr', require => Tcserver::Stop['tcserverstop'],} include tcrestart}"

Sorting Cert Issues
--------------------
On the Master:
puppet cert clean <NODE NAME>

On the Client:
rm -rf $(puppet agent --configprint ssldir)
puppet agent --test --debug

On the Master:
puppet cert sign <NODE NAME>
